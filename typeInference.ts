let a = 123
let b = "123"

a = b; //Error Occured in compile

//type infrence means the typescript inferece variable when inititlize


let c = [1, 2, "john"]
c.push("omid")
c.push(true) //Error in Compile because boolian does not present in variable initilization